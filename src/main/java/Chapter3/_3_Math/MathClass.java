package Chapter3._3_Math;

public class MathClass {
    /**
     * The Math class contains utility methods that can be used for calculations like exponents, rounding, random
     * number generation, etc.
     * The Math class contains static methods, which means that it does not need to be instantiated for a developer
     * to use this methods. This is very important to understand, as new coders often misuse the static keyword.
     * Static methods and variables of Math are associated with the Math class rather than a Math object.
     *
     * @return return a^b using the pow() method of the Math class.
     */
    public double mathPow(double a, double b){
        return 0;
    }
}
