package Chapter4._3_FileIO.WriteListIntoFile;

import java.util.List;

public class WriteListIntoFile {
    /**
     * This is the reverse of the previous problem. Given a filepath and a list of Strings, write the list of strings
     * to the fine. Every item in items should represent a new line in the file.
     * @param filepath the file path you will write to.
     * @param items a list of items to be written to filepath.
     */
    public void write(String filepath, List<String> items){

    }
}
