package Chapter4._0_OOP.StaticKeyword;

public class Runner {
    public void changeClassVar(String in){
        /*
        change the variable global_var to in.
        notice that you're interacting with the CLASS and not an OBJECT.
        Meaning, that there is only a single global_var shared across your whole application, rather than there being
        a var for every object.

        Using the static keyword is an intentional decision. If you are using the static keyword and don't know why,
        you are making a mistake.
         */
    }
}
