package Chapter4._0_OOP.Casting.CastingPrimitive;

public class CastingPrimitive {
    /*
    we can cast between primitive types, too. for instance, casting a double into an int will end up
    dropping all digits after the period!
    try casting d to int here:
     */
    public int castToInt(double d){
        return 0;
    }
}
