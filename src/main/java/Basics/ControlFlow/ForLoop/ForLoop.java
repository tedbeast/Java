package Basics.ControlFlow.ForLoop;

public class ForLoop {
    //    after declaring a sum variable initially starting at 0,
    //    use a for loop starting at i = start, i < end; i++
    //    with a sum = sum + 1; inside of the loop,
    //    to get the sum of all numbers between start and end.
    public int forExample(int start, int end){
        int sum = 0;

//        code here

        return sum;
    }
}
