package Chapter1._2_IfStatement;

public class IfStatement {
    /*
        an if statement uses a conditional statement (within parentheses) to determine if the code
        within {curly braces} should run.
        if statements can be coupled with an else{} statement of else if(){} statement.
        for this example, if bool is true, return x. if bool is false, return y.
        you can consult existing examples to result syntax issues.
     */
    public int ifExample(boolean bool, int x, int y){
        return 0;
    }
}
