package Chapter9.Challenges.CoinCounter;
public class CoinCounter {
    /**
     * given an array of coin values and total value amount, return the smallest number of coins needed to reach
     * the amount.
     * you can assume that there is a valid answer given inputs.
     *
     * for example:
     * with the coin values 3, 7, 5, 1, and a total value of 10, we would need a minimum of 2 coins total.
     * With a total value of 9, we would need a minimum of 3 coins total.
     *
     * You will most likely need to use recursion.
     *
     * @param values the available coin values.
     * @param total the total amount.
     * @return the minimum number of coins needed to sum up to a total value.
     */
    public int count(int[] values, int total){
        return 0;
    }

}
